This prototype project shows the usage of Less (and JSF and/or CDI).

#Problem#
The goal is to assign 'context-aware' Less variables,
so the compiled css is dynamic, and can be used as a templating system.
